buildscript {
    repositories {
        mavenCentral()
        maven {
          url uri('gradleLibs/')
        }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.0.+'
        classpath 'dk.giraf:deployPlugin:1.0'
        classpath 'com.github.triplet.gradle:play-publisher:1.0.2'
    }
}

apply plugin: 'android'
apply plugin: 'dk.giraf.deploy'
apply plugin: 'play'

dependencies {
    compile fileTree(dir: 'libs', include: '*.jar')
    compile project(':pictogram-lib')
    compile project(':OasisLib')
    compile project(':GIRAF_Components')
}

android {
    compileSdkVersion 15
    buildToolsVersion "21.1.2"

    lintOptions {
        abortOnError false
    }
}

play {
    serviceAccountEmail = project.playAccountEmail
    pk12File = file(project.playKeyPath)
    track = 'alpha'
}